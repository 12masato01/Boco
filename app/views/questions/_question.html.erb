<div class="question-container">
  <%= link_to question_path(question), class:"no-underline", method: :get do%>
  <ul>
    <li class="title"><i class="fab fa-quora">.</i><%= question.title %></li>
    <p class="timestamp">
      Posted <%= time_ago_in_words(question.created_at) %> ago.
    </p>
  </ul>
  <% end %>
</div>