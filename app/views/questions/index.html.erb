<%= provide(:title, "質問一覧") %>

<div class="container" id=questions>
  <div class="index-question">
    <% if @search_questions %>
      <%= render partial: "questions/question", collection: @search_questions %>
    <% else %>
       <%= render @questions %>
    <% end %> 
  </div>
</div>